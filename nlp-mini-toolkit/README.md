
A NLP related repo covering:
- Training Word2Vec (SGNS) embeddings and analyzing nearest neighbors / cosine similarity.
- Document classification with averaged static embeddings + logistic regression.
- A character-level GRU language model for name generation (argmax & sampling).
- A Transformer-based sentiment classifier for movie reviews.

Code is organized as standalone scripts with minimal dependencies.
